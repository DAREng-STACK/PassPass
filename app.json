{
  "name": "PassPass",
  "scripts": {
  },
  "env": {
    "JAWSDB_URL": {
      "description": "URL for mysql server",
      "required": true
    },
    "FB_CALLBACKURL": {
      "description": "For FB OAuth",
      "required": true
    },
    "FB_CLIENTID": {
      "description": "For FB OAuth",
      "required": true
    },
    "FB_CLIENTSECRET": {
      "description": "For FB OAuth",
      "required": true
    },
    "SESSION_SECRET": {
      "description": "For server session auth",
      "required": true
    },
    "TWILIO_TOKEN": {
      "description": "Twilio Token",
      "required": true
    },
    "TWILIO_SID": {
      "description": "Twilio accountSID",
      "required": true
    },
    "STRIPE_AUTH": {
      "description": "Strip token",
      "required": true
    }
  },
  "formation": {
  },
  "addons": [

  ],
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ]
}
